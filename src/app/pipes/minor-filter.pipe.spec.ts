import { MinorFilterPipe } from './minor-filter.pipe';

describe('MinorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MinorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
