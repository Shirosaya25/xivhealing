import { AbilityFilterPipe } from './ability-filter.pipe';

describe('AbilityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AbilityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
